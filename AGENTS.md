# Agent instructions

See [CLAUDE.md](CLAUDE.md). It is written for any coding agent working on this
project, not only Claude Code — the build commands, the three test suites, the
traps that cost real time, and the conventions are all there.

This file exists because Codex looks for `AGENTS.md` and Claude Code looks for
`CLAUDE.md`. Keeping one of them a pointer means there is only ever one copy to
keep true.
